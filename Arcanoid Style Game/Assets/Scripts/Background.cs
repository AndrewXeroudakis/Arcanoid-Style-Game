using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Background : MonoBehaviour {

    /*private LevelManager levelManager;

    private SpriteRenderer spriteRenderer;

    public Sprite summerBackground;
    public Sprite autumnBackground;
    public Sprite winterBackground;
    public Sprite springBackground;

    void Start ()
    {
        spriteRenderer = this.GetComponent<SpriteRenderer>();
        levelManager = GameObject.FindObjectOfType<LevelManager>();
        LoadSprites();
    }*/

    public void LoadSprites()
    {
        /*switch (levelManager.season)
        {
            case (int)LevelManager.Season.SUMMER:
                {
                    this.spriteRenderer.sprite = summerBackground;
                }
                break;
            case (int)LevelManager.Season.AUTUMN:
                {
                    this.spriteRenderer.sprite = autumnBackground;
                }
                break;
            case (int)LevelManager.Season.WINTER:
                {
                    this.spriteRenderer.sprite = winterBackground;
                }
                break;
            case (int)LevelManager.Season.SPRING:
                {
                    this.spriteRenderer.sprite = springBackground;
                }
                break;
        }*/
    }
}
